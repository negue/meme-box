import {GroupByTypePipe} from './group-by-type.pipe';

describe('GroupByTypePipe', () => {
  it('create an instance', () => {
    const pipe = new GroupByTypePipe();
    expect(pipe).toBeTruthy();
  });
});
