import {AnimationLabelPipe} from './animation-label.pipe';

describe('AnimationLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new AnimationLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
