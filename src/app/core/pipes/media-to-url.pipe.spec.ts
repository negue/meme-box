import {MediaToUrlPipe} from './media-to-url.pipe';

describe('MediaToUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new MediaToUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
