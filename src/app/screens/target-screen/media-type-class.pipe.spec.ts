import {MediaTypeClassPipe} from './media-type-class.pipe';

describe('MediaTypeClassPipe', () => {
  it('create an instance', () => {
    const pipe = new MediaTypeClassPipe();
    expect(pipe).toBeTruthy();
  });
});
