import {MediaToggleDirective} from './media-toggle.directive';

describe('MediaToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new MediaToggleDirective();
    expect(directive).toBeTruthy();
  });
});
