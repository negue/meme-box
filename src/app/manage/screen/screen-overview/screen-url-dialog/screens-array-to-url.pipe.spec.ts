import {ScreensArrayToUrlPipe} from './screens-array-to-url.pipe';

describe('ScreensArrayToUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new ScreensArrayToUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
