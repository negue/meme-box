import {ActionScreenListPipe} from './action-screen-list.pipe';

describe('ActionScreenListPipe', () => {
  it('create an instance', () => {
    const pipe = new ActionScreenListPipe();
    expect(pipe).toBeTruthy();
  });
});
