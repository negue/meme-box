import { ShowScreenSelectionPipe } from './show-screen-selection.pipe';

describe('ShowScreenSelectionPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowScreenSelectionPipe();
    expect(pipe).toBeTruthy();
  });
});
