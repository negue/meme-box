import {ActionCanPreviewPipe} from './action-can-preview.pipe';

describe('ActionCanPreviewPipe', () => {
  it('create an instance', () => {
    const pipe = new ActionCanPreviewPipe();
    expect(pipe).toBeTruthy();
  });
});
