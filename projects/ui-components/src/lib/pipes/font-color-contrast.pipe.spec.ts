import {FontColorContrastPipe} from './font-color-contrast.pipe';

describe('FontColorContrastPipe', () => {
  it('create an instance', () => {
    const pipe = new FontColorContrastPipe();
    expect(pipe).toBeTruthy();
  });
});
