import {ActionPreviewLabelPipe} from './action-preview-label.pipe';

describe('ActionPreviewLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new ActionPreviewLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
