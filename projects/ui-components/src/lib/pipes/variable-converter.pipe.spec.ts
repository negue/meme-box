import {VariableConverterPipe} from './variable-converter.pipe';

describe('VariableConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new VariableConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
