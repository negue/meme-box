import { ActionVariableConfigPipe } from './action-variable-config.pipe';

describe('ActionVariableConfigPipe', () => {
  it('create an instance', () => {
    const pipe = new ActionVariableConfigPipe();
    expect(pipe).toBeTruthy();
  });
});
